# OPEN-DECISIONS · 悬而未决登记册

> **规则**：出现「定不下来 / 先放一放 / 等外部条件」的事，**立即**在这里落条；
> 只追加 + 就地关闭（OPEN → RESOLVED，补 Resolution 字段），**不删除、不覆盖**。
> 每次开工前先扫一遍本表，逐条判断能否关闭。
> 关闭的条目可升格为 ADR（见同目录 `ADR-00X-*.md`）。

**三类固定 slug**
- `waiting-on-external-condition` —— 等外部条件（老大拍板 / 第三方能力）
- `design-decision-to-evaluate` —— 设计待评估（需对比或试点）
- `existing-design-boundary` —— 现有设计的边界约束（不是缺陷，是已知取舍）

| Date | Source | Open Item | Slug | Related Constraints | Current Leaning | Blocked By | Resolves When | Status |
|------|--------|-----------|------|---------------------|-----------------|------------|---------------|--------|
| 2026-09-22 | 复核 | **正文类 AI（续写/扩写/剧情走向/校对）做不做** | waiting-on-external-condition | 需求文档强制声明「记忆管理工具，而非写作 AI」「AI 只在想的环节帮忙，**不在写的环节代笔**」 | 倾向只做「剧情走向 + 校对」两个**不代笔**的；要真代笔就得**同步改需求文档那句声明**，不偷着做 | 老大定性 | 老大拍板后 | OPEN |
| 2026-09-22 | 复核 | **一个模型能否承担多个任务角色** | design-decision-to-evaluate | `llm_provider.task_role` 是单值字段 + CHECK 只约束取值、**不保证角色唯一** | 先「理顺现有配置 + 界面如实提示多占位」，中长期拆出「角色 → 模型」关联表 | 需先定正文 AI 范围（否则要返工） | 正文类 AI 定型后 | OPEN |
| 2026-09-22 | 复核 | **语义召回长期为空：embedding 模型从哪来** | waiting-on-external-condition | 现有平台只有聊天模型；「数据只在本机」是产品卖点 | 首选本地向量模型（不被第三方看到稿子）；定不了就先明确「未启用」而不是每章显示 0 | 老大给方案（另给 key / 本地跑 / 明确关闭） | 老大拍板后 | OPEN |
| 2026-09-22 | 复核 | **一致性审校 R8 是否解冻** | existing-design-boundary | 契约标 `x-m1-scope: deferred`，是全项目唯一差集；`task_role=review` 目前**无任何代码消费** | 保持冻结，界面上如实标「未启用」（本轮已做） | 取决于正文 AI 排期 | 排期确定后 | OPEN |
| 2026-09-22 | 复核 | **选题数据策略：写死 / 联网 / 混合 / 用户维护** | design-decision-to-evaluate | 架构是「纯本地读文件、绝不联网」；`genres.json` 22 题材、`benchmarks` 全空 | 短期只补真需要的字段；中长期「本地定结构 + 联网补增量」+ 允许用户自维护 | 需要引入联网能力，与现有设计冲突 | 老大拍板后 | OPEN |
| 2026-09-22 | 复核 | **开书是否改成「骨架优先」向导** | waiting-on-external-condition | 实战指南六阶段是「立项 → 骨架 → 包装 → 写稿」，现状是「新建 → 填书名 → 空白写作台」 | 本轮已用**最小改动**补引导（空章节时指路到设定库/大纲），不做新向导 | 老大拍板是否要完整向导 | 老大拍板后 | OPEN |
| 2026-09-22 | 复核 | **AI 对话是否升级为后端会话表** | design-decision-to-evaluate | 本轮已做**前端本地留存**（按作品分键，关掉再打开还在） | 先用本地留存观察；确需跨设备同步再上后端表 + 迁移 | 需先确认会话量真的需要跨设备 | 出现跨设备诉求时 | OPEN |
| 2026-09-22 | 复核 | **书库里遗留的旧 `llm_provider` 表要不要 DROP** | existing-design-boundary | 该表已全局化到 `data/app.db`；书库那份是**刻意保留**的迁移源（`provider_migration.py` 注释写明"旧库按设计保留、不删"），且已有回归测试断言新书库不含此表 | **不删**：删了就丢掉老用户升级时的迁移来源；靠测试锁死"没有代码读它" | 需确认不再需要从旧库迁移 | 无老书库待迁移时 | OPEN |
| 2026-09-22 | 复核 | **契约路径参数名与实现不一致** | existing-design-boundary | 契约统一写 `{id}`，实现用 `{char_id}` / `{chapter_id}` / `{provider_id}`（URL 形状一致，仅名字不同） | **不改**：要动 16 个路由的处理函数签名，零功能收益、高回归风险 | 无 | 若将来生成客户端报错再评估 | OPEN |
| 2026-09-22 | 复核 | **FTS5 用 `unicode61` 分词，中文子串 MATCH 恒为 0** | existing-design-boundary | 功能仍正确（自动降级 LIKE），只是性能问题；改它要动表结构 + 迁移存量书 | 按既定计划 **M3 发布前**处理 | 需先做 M2 结论 | M3 前 | OPEN |
| 2026-09-22 | 复核 | **`_清理_20260922/`（65.7 MB）与 `.venv.broken/`（30.2 MB）是否删除** | waiting-on-external-condition | C 盘紧张；两者都是**可恢复归档**，未直删 | 建议按「只删确定无价值的部分」处理：删缓存与调试截图、**保留** M2 小说版本链 | 老大确认 | 老大拍板后 | OPEN |
| 2026-09-22 | 复核 | **小说《重生之我在末世开超市》2 处遗留** | waiting-on-external-condition | ① ch5 父亲墙上「倒计时 78 天」与父亲 2021 年去世的先天矛盾；② ch4 引入的西郊仓库与 ch2 的 7 号仓库未合并 | 若这本只作验证样本 → 不必改；若要当成品 → 先建 git 回滚点（**已完成**）再改 | 老大确认是否要交付这本 | 老大拍板后 | OPEN |

---

## 已关闭

> 关闭时补 `Resolution`。目前为空 —— 本表建于 2026-09-22 第 2 次对话。

（无）
