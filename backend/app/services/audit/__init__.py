"""质检（audit）子模块：去 AI 味本地规则 + 敏感词本地词库匹配。

两个功能都**不依赖 AI 模型、不联网**，因此在不配置模型时也必须可用（项目红线 3）。

- `rules_ai_flavor`：去 AI 味词表与评分配置（数据）。
- `ai_flavor`：去 AI 味本地规则检测（纯函数）。
- `aho_corasick`：多模式串匹配自动机（纯算法）。
- `normalize`：敏感词匹配前的正文归一化（纯函数）。
- `wordlist`：敏感词库解析、自动机构建与缓存。
- `sensitive`：把词库应用到章节正文的扫描逻辑。
"""
