node_modules
dist-ssr
*.local
.vite
.DS_Store
npm-debug.log*

# ============================================================================
# 注意：`dist/` **故意不在这里忽略** —— 这是一处刻意的设计，不是漏写。
# ============================================================================
# 理由：本工具的定位是「双击即用」。
#   · 运行阶段**完全不需要 Node.js**，前端构建产物随仓库分发；
#   · 克隆/解压下来直接跑 start.bat（或 ./start.sh），bootstrap.py 会校验
#     `frontend/dist/index.html` 是否存在并启动服务。
#
# 历史问题（2026-09-22 发现并修正）：
#   本文件原本有一行 `dist`（Vite 模板默认），导致 dist 不进仓库；
#   而根 .gitignore 的注释与 README「首次安装」一节都写着
#   "构建产物 dist/ 已随仓库分发、运行阶段不需要 Node.js" ——
#   两者直接矛盾，结果是**别人克隆下来只会看到"缺少前端构建产物"，
#   必须装 Node 自己 build 一次**，"开箱即用"的承诺失效。
#
# 改动前请先对齐三处口径：本文件、根 .gitignore、README「首次安装」一节。
# 若你确实不想提交构建产物，那三处必须同时改成"需要 Node.js 构建一次"。
